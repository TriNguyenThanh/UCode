<UserControl x:Class="UCode.Desktop.Pages.TeacherHomePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1600"
             Background="#F5F5F5">

    <!-- Loading Overlay -->
    <Grid>
        <Grid x:Name="LoadingOverlay" Background="#80000000" 
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" 
              Panel.ZIndex="1000">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Foreground="#FACB01"/>
                <TextBlock Text="Đang tải dữ liệu..." Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,15,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Welcome Section -->
            <Border Grid.Row="0" Background="White" Padding="40,30">
                <StackPanel>
                    <TextBlock FontSize="32" FontWeight="Bold" Foreground="#191970" Margin="0,0,0,5">
                        <Run Text="Xin chào, "/>
                        <Run Text="{Binding TeacherName}"/>
                        <Run Text="! 👨‍🏫"/>
                    </TextBlock>
                    <TextBlock Text="Quản lý lớp học, bài tập và đánh giá sinh viên" 
                               Foreground="#666" FontSize="16"/>
                </StackPanel>
            </Border>

            <!-- Main Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="40,30,40,40">
                    
                    <!-- Stats Cards Row -->
                    <Grid Margin="0,0,0,30">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Classes Card -->
                        <Border Grid.Column="0" Background="#FACB01" CornerRadius="8" Padding="25" Margin="0,0,12,0">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.15"/>
                            </Border.Effect>
                            <StackPanel>
                                <Grid Margin="0,0,0,15">
                                    <TextBlock Text="Lớp học" FontSize="16" FontWeight="SemiBold" 
                                               Foreground="#191970" HorizontalAlignment="Left"/>
                                    <TextBlock Text="📚" FontSize="28" HorizontalAlignment="Right"/>
                                </Grid>
                                <TextBlock Text="{Binding TotalClasses}" FontSize="56" FontWeight="Bold" 
                                           Foreground="#191970" Margin="0,0,0,5"/>
                                <TextBlock Text="Đang giảng dạy" FontSize="16" Foreground="#191970"/>
                            </StackPanel>
                        </Border>

                        <!-- Students Card -->
                        <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="25" Margin="6,0">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.1"/>
                            </Border.Effect>
                            <StackPanel>
                                <Grid Margin="0,0,0,15">
                                    <TextBlock Text="Sinh viên" FontSize="16" FontWeight="SemiBold" 
                                               Foreground="#666" HorizontalAlignment="Left"/>
                                    <TextBlock Text="👥" FontSize="28" Foreground="#17a2b8" HorizontalAlignment="Right"/>
                                </Grid>
                                <TextBlock Text="{Binding TotalStudents}" FontSize="56" FontWeight="Bold" 
                                           Foreground="#17a2b8" Margin="0,0,0,5"/>
                                <TextBlock Text="Tổng số sinh viên" FontSize="16" Foreground="#666"/>
                            </StackPanel>
                        </Border>

                        <!-- Assignments Card -->
                        <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="25" Margin="6,0">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.1"/>
                            </Border.Effect>
                            <StackPanel>
                                <Grid Margin="0,0,0,15">
                                    <TextBlock Text="Bài tập" FontSize="16" FontWeight="SemiBold" 
                                               Foreground="#666" HorizontalAlignment="Left"/>
                                    <TextBlock Text="📝" FontSize="28" Foreground="#28a745" HorizontalAlignment="Right"/>
                                </Grid>
                                <TextBlock Text="{Binding ActiveAssignments}" FontSize="56" FontWeight="Bold" 
                                           Foreground="#28a745" Margin="0,0,0,5"/>
                                <TextBlock Text="Đang hoạt động" FontSize="16" Foreground="#666"/>
                            </StackPanel>
                        </Border>

                        <!-- Pending Grading Card -->
                        <Border Grid.Column="3" Background="White" CornerRadius="8" Padding="25" Margin="12,0,0,0">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.1"/>
                            </Border.Effect>
                            <StackPanel>
                                <Grid Margin="0,0,0,15">
                                    <TextBlock Text="Chờ chấm" FontSize="16" FontWeight="SemiBold" 
                                               Foreground="#666" HorizontalAlignment="Left"/>
                                    <TextBlock Text="📋" FontSize="28" Foreground="#ffc107" HorizontalAlignment="Right"/>
                                </Grid>
                                <TextBlock Text="{Binding PendingGrading}" FontSize="56" FontWeight="Bold" 
                                           Foreground="#ffc107" Margin="0,0,0,5"/>
                                <TextBlock Text="Bài nộp chưa chấm" FontSize="16" Foreground="#666"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Classes Section -->
                    <StackPanel Margin="0,0,0,40">
                        <Grid Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <TextBlock Text="📚" FontSize="20" Foreground="#191970" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Lớp học của tôi" FontSize="22" FontWeight="Bold" 
                                           Foreground="#191970" VerticalAlignment="Center"/>
                            </StackPanel>
                            <Button Command="{Binding CreateClassCommand}" HorizontalAlignment="Right"
                                    Background="#FACB01" Foreground="#191970" BorderThickness="0"
                                    FontWeight="Bold" FontSize="16" Padding="20,12" Cursor="Hand">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="➕ " FontSize="16" Margin="0,0,5,0"/>
                                    <TextBlock Text="TẠO LỚP MỚI"/>
                                </StackPanel>
                            </Button>
                        </Grid>

                        <ItemsControl ItemsSource="{Binding Classes}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="4"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="White" CornerRadius="8" Margin="0,0,15,15" 
                                            BorderThickness="1" BorderBrush="#E0E0E0" Cursor="Hand">
                                        <Border.Effect>
                                            <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.1"/>
                                        </Border.Effect>
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="BorderBrush" Value="#003d8f"/>
                                                        <Setter Property="RenderTransform">
                                                            <Setter.Value>
                                                                <TranslateTransform Y="-4"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect ShadowDepth="4" BlurRadius="12" Opacity="0.2"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Border.InputBindings>
                                            <MouseBinding Command="{Binding DataContext.ViewClassCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          CommandParameter="{Binding ClassId}"
                                                          MouseAction="LeftClick"/>
                                        </Border.InputBindings>
                                        <StackPanel>
                                            <!-- Class Code Header -->
                                            <Border Height="150" CornerRadius="8,8,0,0">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                        <GradientStop Color="#001f54" Offset="0"/>
                                                        <GradientStop Color="#003d8f" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                                <TextBlock Text="{Binding Code}" FontSize="32" FontWeight="Bold" 
                                                           Foreground="#FACB01" HorizontalAlignment="Center" 
                                                           VerticalAlignment="Center" TextAlignment="Center"/>
                                            </Border>

                                            <!-- Class Info -->
                                            <StackPanel Margin="20">
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                                    <TextBlock Text="👥" FontSize="16" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                                    <TextBlock FontSize="16" Foreground="#666" VerticalAlignment="Center">
                                                        <Run Text="{Binding StudentCount}"/>
                                                        <Run Text=" SV"/>
                                                    </TextBlock>
                                                </StackPanel>
                                                <Border Background="Transparent" BorderBrush="#E0E0E0" BorderThickness="1" 
                                                        CornerRadius="12" Padding="8,4" HorizontalAlignment="Left">
                                                    <TextBlock Text="5 bài tập" FontSize="13" Foreground="#666"/>
                                                </Border>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <!-- Recent Assignments Section -->
                    <StackPanel>
                        <Grid Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <TextBlock Text="📚" FontSize="20" Foreground="#191970" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="Bài tập gần đây" FontSize="22" FontWeight="Bold" 
                                           Foreground="#191970" VerticalAlignment="Center"/>
                            </StackPanel>
                            <Button Content="→" Command="{Binding ViewAllAssignmentsCommand}" 
                                    Background="White" Foreground="#191970" BorderThickness="1" BorderBrush="#E0E0E0"
                                    Width="40" Height="40" Padding="0" FontSize="20"
                                    HorizontalAlignment="Right" Cursor="Hand"/>
                        </Grid>

                        <ItemsControl ItemsSource="{Binding RecentAssignments}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="White" CornerRadius="8" Margin="0,0,0,15" 
                                            BorderThickness="1" BorderBrush="#E0E0E0" Cursor="Hand">
                                        <Border.Effect>
                                            <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.1"/>
                                        </Border.Effect>
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="BorderBrush" Value="#003d8f"/>
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect ShadowDepth="3" BlurRadius="10" Opacity="0.15"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Border.InputBindings>
                                            <MouseBinding Command="{Binding DataContext.ViewAssignmentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          CommandParameter="{Binding AssignmentId}"
                                                          MouseAction="LeftClick"/>
                                        </Border.InputBindings>
                                        <Grid Margin="25">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Icon -->
                                            <Border Grid.Column="0" Width="60" Height="60" Background="#FACB01" 
                                                    CornerRadius="8" Margin="0,0,25,0">
                                                <TextBlock Text="📚" FontSize="32" 
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>

                                            <!-- Content -->
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" 
                                                           Foreground="#191970" Margin="0,0,0,5"/>
                                                <TextBlock Text="{Binding ClassName}" FontSize="16" Foreground="#666"/>
                                            </StackPanel>

                                            <!-- Actions -->
                                            <StackPanel Grid.Column="2" VerticalAlignment="Center" Orientation="Horizontal">
                                                <Button Command="{Binding DataContext.GradeAssignmentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding AssignmentId}"
                                                        Background="#FACB01" Foreground="#191970" BorderThickness="0"
                                                        FontWeight="Bold" FontSize="15" Padding="18,10"
                                                        Cursor="Hand" Margin="0,0,12,0">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="📝 " FontSize="16" Margin="0,0,5,0"/>
                                                        <TextBlock Text="CHẤM BÀI"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Command="{Binding DataContext.ViewReportCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding AssignmentId}"
                                                        Background="Transparent" Foreground="#191970" BorderThickness="0"
                                                        FontWeight="SemiBold" FontSize="15" Padding="18,10" 
                                                        Cursor="Hand">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="👁 " FontSize="16" Margin="0,0,5,0"/>
                                                        <TextBlock Text="XEM BÁO CÁO"/>
                                                    </StackPanel>
                                                </Button>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
