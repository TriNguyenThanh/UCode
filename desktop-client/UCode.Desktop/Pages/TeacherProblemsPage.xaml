<UserControl x:Class="UCode.Desktop.Pages.TeacherProblemsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="White">

    <UserControl.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0056B3"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FilterButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#6C757D"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F8F9FA"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ActiveFilterButtonStyle" TargetType="Button" BasedOn="{StaticResource FilterButtonStyle}">
            <Setter Property="Background" Value="#007BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#007BFF"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&lt;/&gt;" Foreground="#007BFF" FontSize="24" FontWeight="Bold" Margin="0,0,10,0"/>
                    <TextBlock Text="Ngân hàng bài" Foreground="Black" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
                <TextBlock Text="Quản lý và tạo bài tập cho sinh viên" Foreground="#6C757D" FontSize="14" Margin="0,5,0,0"/>
            </StackPanel>

            <Button Grid.Column="1" Content="+ Tạo bài mới" Style="{StaticResource PrimaryButtonStyle}" 
                    Command="{Binding CreateProblemCommand}"/>
        </Grid>

        <!-- Stats Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Total Problems Card -->
            <Border Grid.Column="0" Style="{StaticResource StatCardStyle}" Margin="0,0,15,0">
                <StackPanel>
                    <TextBlock Text="Tổng số bài" Foreground="#6C757D" FontSize="13" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding TotalProblems}" Foreground="Black" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Easy Card -->
            <Border Grid.Column="1" Style="{StaticResource StatCardStyle}" Margin="0,0,15,0">
                <StackPanel>
                    <TextBlock Text="Dễ" Foreground="#6C757D" FontSize="13" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding EasyProblems}" Foreground="#28A745" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Medium Card -->
            <Border Grid.Column="2" Style="{StaticResource StatCardStyle}" Margin="0,0,15,0">
                <StackPanel>
                    <TextBlock Text="Trung bình" Foreground="#6C757D" FontSize="13" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding MediumProblems}" Foreground="#FFC107" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Hard Card -->
            <Border Grid.Column="3" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="Khó" Foreground="#6C757D" FontSize="13" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding HardProblems}" Foreground="#DC3545" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Search and Filters -->
        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Search Box -->
            <Border Grid.Row="0" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6" Padding="12,10" Background="White" Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="🔍" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0" Opacity="0.5"/>
                    <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0" Background="Transparent" VerticalContentAlignment="Center"
                             FontSize="14">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Foreground" Value="Black"/>
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Foreground" Value="#6C757D"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Grid>
            </Border>

            <!-- Filter Buttons -->
            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <Button Content="Tất cả" Command="{Binding FilterAllCommand}" Margin="0,0,5,0">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource FilterButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsFilterAll}" Value="True">
                                    <Setter Property="Background" Value="#007BFF"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderBrush" Value="#007BFF"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="Dễ" Command="{Binding FilterEasyCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource FilterButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsFilterEasy}" Value="True">
                                    <Setter Property="Background" Value="#007BFF"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderBrush" Value="#007BFF"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="Trung bình" Command="{Binding FilterMediumCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource FilterButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsFilterMedium}" Value="True">
                                    <Setter Property="Background" Value="#007BFF"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderBrush" Value="#007BFF"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="Khó" Command="{Binding FilterHardCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource FilterButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsFilterHard}" Value="True">
                                    <Setter Property="Background" Value="#007BFF"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderBrush" Value="#007BFF"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Problems Table -->
        <Border Grid.Row="3" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Background="White">
            <DataGrid ItemsSource="{Binding Problems}" 
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      CanUserSortColumns="True"
                      CanUserResizeColumns="True"
                      BorderThickness="0"
                      Background="Transparent"
                      RowHeight="60">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#6C757D"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="Padding" Value="20,15"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#F0F0F0"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F8F9FA"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="20,0" Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.Columns>
                    <!-- Code / Title Column -->
                    <DataGridTemplateColumn Header="Mã / Tiêu đề" Width="2.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Code}" Foreground="#6C757D" FontSize="12" Margin="0,0,0,3"/>
                                    <TextBlock Text="{Binding Title}" Foreground="#007BFF" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding FilePath}" Foreground="#6C757D" FontSize="11" Opacity="0.7" Margin="0,2,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Difficulty Column -->
                    <DataGridTemplateColumn Header="Độ khó" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12" Padding="12,6" HorizontalAlignment="Left">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Difficulty}" Value="Dễ">
                                                    <Setter Property="Background" Value="#28A745"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Difficulty}" Value="Trung bình">
                                                    <Setter Property="Background" Value="#FFC107"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Difficulty}" Value="Khó">
                                                    <Setter Property="Background" Value="#DC3545"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Difficulty}" Foreground="White" 
                                               FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Visibility Column -->
                    <DataGridTemplateColumn Header="Visibility" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#E8F5E9" CornerRadius="12" Padding="12,6" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Visibility}" Foreground="#28A745" 
                                               FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Tags Column -->
                    <DataGridTemplateColumn Header="Tags" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Tags}" Foreground="#6C757D" FontSize="13"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Time Limit Column -->
                    <DataGridTemplateColumn Header="Giới hạn" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TimeLimit}" Foreground="#6C757D" FontSize="13"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Actions Column -->
                    <DataGridTemplateColumn Header="Thao tác" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="✏️" ToolTip="Chỉnh sửa"
                                            Command="{Binding DataContext.EditProblemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding ProblemId}"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                                            FontSize="16" Padding="8" Margin="0,0,5,0"/>
                                    <Button Content="🗑️" ToolTip="Xóa"
                                            Command="{Binding DataContext.DeleteProblemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding ProblemId}"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                                            FontSize="16" Padding="8"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Pagination -->
        <Grid Grid.Row="4" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left">
                <Run Text="Hiển thị trang " Foreground="#6C757D" FontSize="13"/>
                <Run Text="{Binding CurrentPage}" Foreground="Black" FontWeight="SemiBold" FontSize="13"/>
                <Run Text=" / " Foreground="#6C757D" FontSize="13"/>
                <Run Text="{Binding TotalPages}" Foreground="Black" FontWeight="SemiBold" FontSize="13"/>
                <Run Text=" (Tổng " Foreground="#6C757D" FontSize="13"/>
                <Run Text="{Binding TotalProblems}" Foreground="Black" FontWeight="SemiBold" FontSize="13"/>
                <Run Text=" bài)" Foreground="#6C757D" FontSize="13"/>
            </TextBlock>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="← Trang trước" Command="{Binding PreviousPageCommand}"
                        Style="{StaticResource FilterButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="Trang sau →" Command="{Binding NextPageCommand}"
                        Style="{StaticResource FilterButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
