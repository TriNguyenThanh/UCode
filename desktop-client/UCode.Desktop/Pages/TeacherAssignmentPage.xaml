<UserControl x:Class="UCode.Desktop.Pages.TeacherAssignmentPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#F5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Breadcrumb -->
        <StackPanel Grid.Row="0" Margin="30,20,30,0">
            <TextBlock FontSize="13" Foreground="#666">
                <Run Text="Lớp học / Practice Session - Algorithms"/>
            </TextBlock>
        </StackPanel>

        <!-- Header -->
        <Border Grid.Row="1" Padding="30,20,30,25">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="{Binding Assignment.Title}" Foreground="#003A70" FontSize="32" FontWeight="Bold" Margin="0,0,0,15"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Border Background="#003A70" CornerRadius="12" Padding="10,5" Margin="0,0,10,0">
                            <TextBlock Foreground="White" FontWeight="SemiBold" FontSize="12">
                                <Run Text="✓ Đã nộp: "/>
                                <Run Text="{Binding Statistics.Submitted, FallbackValue=0}"/>
                                <Run Text="/"/>
                                <Run Text="{Binding Statistics.TotalStudents, FallbackValue=0}"/>
                            </TextBlock>
                        </Border>
                        <Border Background="#28a745" CornerRadius="12" Padding="10,5" Margin="0,0,10,0">
                            <TextBlock Foreground="White" FontWeight="SemiBold" FontSize="12">
                                <Run Text="Đã chấm: "/>
                                <Run Text="{Binding Statistics.Graded, FallbackValue=0}"/>
                            </TextBlock>
                        </Border>
                        <Border Background="#ffc107" CornerRadius="12" Padding="10,5" Margin="0,0,10,0">
                            <TextBlock Foreground="White" FontWeight="SemiBold" FontSize="12">
                                <Run Text="Chưa chấm: "/>
                                <Run Text="{Binding Statistics.Submitted, FallbackValue=0}"/>
                            </TextBlock>
                        </Border>
                        <Border Background="#6c757d" CornerRadius="12" Padding="10,5">
                            <TextBlock Text="{Binding EndTimeDisplay, StringFormat='Hạn: {0}'}" Foreground="White" FontWeight="SemiBold" FontSize="12"/>
                        </Border>
                    </StackPanel>
                    <TextBlock Text="{Binding Assignment.Description}" FontSize="14" Foreground="#666" TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                    <Button Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" Padding="12,8"
                            Command="{Binding EditAssignmentCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="✏" FontSize="16" Margin="0,0,5,0"/>
                            <TextBlock Text="CHỈNH SỬA" FontSize="13" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource PrimaryButtonStyle}" Padding="12,8"
                            Command="{Binding GradeAssignmentCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📊" FontSize="16" Margin="0,0,5,0"/>
                            <TextBlock FontSize="13" FontWeight="SemiBold">
                                <Run Text="CHẤM BÀI ("/>
                                <Run Text="{Binding Statistics.Submitted, FallbackValue=0}"/>
                                <Run Text=")"/>
                            </TextBlock>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource DangerButtonStyle}" Margin="10,0,0,0" Padding="12,8">
                        <TextBlock Text="XÓA" FontSize="13" FontWeight="SemiBold"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Statistics Cards -->
        <Grid Grid.Row="2" Margin="30,0,30,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,7.5,0">
                <StackPanel>
                    <TextBlock Text="Tổng SV" FontSize="12" Foreground="#666" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding Statistics.TotalStudents}" FontSize="28" FontWeight="Bold" Foreground="#191970"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="3.75,0">
                <StackPanel>
                    <TextBlock Text="Chưa làm" FontSize="12" Foreground="#666" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding Statistics.NotStarted}" FontSize="28" FontWeight="Bold" Foreground="#6c757d"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="3.75,0">
                <StackPanel>
                    <TextBlock Text="Đang làm" FontSize="12" Foreground="#666" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding Statistics.InProgress}" FontSize="28" FontWeight="Bold" Foreground="#ffc107"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource CardStyle}" Margin="3.75,0">
                <StackPanel>
                    <TextBlock Text="Đã nộp" FontSize="12" Foreground="#666" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding Statistics.Submitted}" FontSize="28" FontWeight="Bold" Foreground="#17a2b8"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="4" Style="{StaticResource CardStyle}" Margin="7.5,0,0,0">
                <StackPanel>
                    <TextBlock Text="Đã chấm" FontSize="12" Foreground="#666" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding Statistics.Graded}" FontSize="28" FontWeight="Bold" Foreground="#28a745"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Tabs Content -->
        <Grid Grid.Row="3" Margin="30,0,30,30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Tab Headers -->
            <Border Grid.Row="0" BorderThickness="0,0,0,2" BorderBrush="#E0E0E0" Margin="0,10,0,10" RenderTransformOrigin="0.5,0.407">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <Button x:Name="ProblemsTab" Content="DANH SÁCH BÀI" 
                            Foreground="#FACB01" Background="Transparent" BorderThickness="0,0,0,3" BorderBrush="#FACB01"
                            FontSize="13" FontWeight="Bold" Padding="0,10" Height="45" Cursor="Hand" Margin="0,0,20,0"
                            Click="ProblemsTab_Click"/>
                    <Button x:Name="SubmissionsTab" Content="BÀI NỘP SINH VIÊN" 
                            Foreground="#666" Background="Transparent" BorderThickness="0"
                            FontSize="13" FontWeight="Bold" Padding="0,10" Height="45" Cursor="Hand" Margin="0,0,20,0"
                            Click="SubmissionsTab_Click"/>
                </StackPanel>
            </Border>

            <!-- Tab Content -->
            <Grid Grid.Row="1">
                <!-- Problems Tab Content -->
                <Grid x:Name="ProblemsTabContent" Visibility="Visible">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" FontSize="18" FontWeight="Bold" Foreground="#003A70" VerticalAlignment="Center">
                            <Run Text="Danh sách bài ("/>
                            <Run Text="{Binding ProblemsCount, FallbackValue=0}"/>
                            <Run Text=" bài)"/>
                        </TextBlock>
                    </Grid>

                    <Border Grid.Row="1" Style="{StaticResource RoundedDataGridContainer}">
                        <DataGrid ItemsSource="{Binding Problems}"
                                  Style="{StaticResource ModernDataGrid}"
                                  RowStyle="{StaticResource ModernDataGridRow}"
                                  CellStyle="{StaticResource ModernDataGridCell}"
                                  ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                                  RowHeight="60">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="STT" Binding="{Binding OrderIndex}" Width="80"/>
                                <DataGridTextColumn Header="Tên bài" Binding="{Binding Title}" Width="*"/>
                                <DataGridTemplateColumn Header="Độ khó" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="4" Padding="8,4" HorizontalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Difficulty}" Value="EASY">
                                                                <Setter Property="Background" Value="#34C759"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Difficulty}" Value="MEDIUM">
                                                                <Setter Property="Background" Value="#FF9500"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Difficulty}" Value="HARD">
                                                                <Setter Property="Background" Value="#FF3B30"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding Difficulty}" Foreground="White" FontWeight="SemiBold" FontSize="11"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Điểm" Binding="{Binding Points}" Width="100"/>
                                <DataGridTemplateColumn Header="Thao tác" Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="🗑" Background="Transparent" BorderThickness="0" 
                                                    Foreground="#666" FontSize="16" Cursor="Hand"
                                                    ToolTip="Xóa"
                                                    Command="{Binding DataContext.ViewProblemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding ProblemId}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>

                <!-- Submissions Tab Content -->
                <Border x:Name="SubmissionsTabContent" Style="{StaticResource RoundedDataGridContainer}" Visibility="Collapsed">
                    <DataGrid ItemsSource="{Binding Students}"
                              Style="{StaticResource ModernDataGrid}"
                              RowStyle="{StaticResource ModernDataGridRow}"
                              CellStyle="{StaticResource ModernDataGridCell}"
                              ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                              RowHeight="60">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="MSSV" Binding="{Binding StudentCode}" Width="100"/>
                            <DataGridTextColumn Header="Thông tin SV" Binding="{Binding FullName}" Width="*"/>
                            <DataGridTemplateColumn Header="Trạng thái" Width="140">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="4" Padding="8,4" Background="{Binding StatusColor}">
                                            <TextBlock Text="{Binding Status}" Foreground="White" FontWeight="SemiBold" FontSize="11" HorizontalAlignment="Center"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Thời gian bắt đầu" Binding="{Binding StartedAt, StringFormat='dd/MM/yyyy HH:mm'}" Width="150"/>
                            <DataGridTextColumn Header="Điểm" Binding="{Binding ScoreDisplay}" Width="100"/>
                            <DataGridTemplateColumn Header="Thao tác" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Chấm bài" Style="{StaticResource SecondaryButtonStyle}" 
                                                Padding="8,4" FontSize="12"
                                                Command="{Binding DataContext.ViewStudentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding UserId}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

         
            </Grid>
        </Grid>
    </Grid>
</UserControl>
