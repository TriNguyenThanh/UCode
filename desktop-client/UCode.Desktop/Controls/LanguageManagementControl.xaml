<UserControl x:Class="UCode.Desktop.Controls.LanguageManagementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Ngôn ngữ lập trình được phép" 
                       FontSize="18" FontWeight="Bold" Foreground="#191970" VerticalAlignment="Center"/>

            <Button Grid.Column="1" Content="Quản lý ngôn ngữ" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding OpenLanguageDialogCommand}"
                    Padding="15,8"/>
        </Grid>

        <!-- Languages Display -->
        <Grid Grid.Row="1">
            <!-- Empty State -->
            <Border Background="#FFF3CD" BorderBrush="#FF9500" BorderThickness="1" 
                    CornerRadius="4" Padding="20" 
                    Visibility="{Binding HasNoLanguages, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="⚠" Foreground="#FF9500" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Chưa có ngôn ngữ nào được kích hoạt. Nhấn 'Quản lý ngôn ngữ' để thêm." 
                               Foreground="#856404" FontSize="14" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <!-- Languages Table -->
            <Border Style="{StaticResource CardStyle}" Padding="0" 
                    Visibility="{Binding HasLanguages, Converter={StaticResource BooleanToVisibilityConverter}}">
                <DataGrid ItemsSource="{Binding ProblemLanguages}"
                          Style="{StaticResource ModernDataGrid}"
                          RowStyle="{StaticResource ModernDataGridRow}"
                          CellStyle="{StaticResource ModernDataGridCell}"
                          ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}">
                    
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Ngôn ngữ" Binding="{Binding LanguageDisplayName}" Width="*"/>
                        <DataGridTextColumn Header="Code" Binding="{Binding LanguageCode}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Background" Value="#F0F0F0"/>
                                    <Setter Property="Padding" Value="5,2"/>
                                    <Setter Property="FontFamily" Value="Consolas"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Time Factor" Width="120">
                            <DataGridTextColumn.Binding>
                                <Binding Path="TimeFactor" StringFormat="{}{0:0.00}x"/>
                            </DataGridTextColumn.Binding>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Memory (KB)" Width="120">
                            <DataGridTextColumn.Binding>
                                <Binding Path="MemoryKb" StringFormat="{}{0:N0}"/>
                            </DataGridTextColumn.Binding>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="Thao tác" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="✏ Sửa" 
                                            Foreground="#007bff"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            Padding="5"
                                            Command="{Binding DataContext.EditLanguageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</UserControl>

