<UserControl x:Class="UCode.Desktop.Controls.TagManagementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock FontSize="18" FontWeight="Bold" Foreground="#191970">
                    <Run Text="Tags ("/>
                    <Run Text="{Binding CurrentTags.Count, Mode=OneWay}"/>
                    <Run Text=")"/>
                </TextBlock>
            </StackPanel>

            <Button Grid.Column="1" Content="+ Quản lý Tags" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding OpenTagDialogCommand}"
                    Padding="15,8"/>
        </Grid>

        <!-- Tags Display -->
        <Grid Grid.Row="1">
            <!-- Empty State -->
            <Border Background="#F0F8FF" BorderBrush="#2196F3" BorderThickness="1" 
                    CornerRadius="4" Padding="20" Visibility="{Binding HasNoTags, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="ℹ" Foreground="#2196F3" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Chưa có tag nào được gán cho bài toán này. Nhấn 'Quản lý Tags' để thêm tags." 
                               Foreground="#1976D2" FontSize="14" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <!-- Tags List -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" 
                         Visibility="{Binding HasTags, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ItemsControl ItemsSource="{Binding CurrentTags}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#FACB01" CornerRadius="15" Padding="12,6" Margin="0,0,8,8">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding}" Foreground="White" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center"/>
                                    <Button Content="×" Foreground="White" Background="Transparent" 
                                            BorderThickness="0" Margin="8,0,0,0" Cursor="Hand"
                                            FontSize="18" FontWeight="Bold"
                                            VerticalAlignment="Center"
                                            Padding="4,0"
                                            Command="{Binding DataContext.RemoveTagCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Opacity" Value="0.8"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>

