<mah:MetroWindow x:Class="UCode.Desktop.Views.CreateAssignmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        Title="Tạo bài tập mới - UCode" 
        Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F8FAFC"
        ResizeMode="NoResize"
        ShowMinButton="False"
        ShowMaxRestoreButton="False"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        RenderOptions.BitmapScalingMode="HighQuality">

    <Grid>
        <!-- Loading Overlay -->
        <Grid x:Name="LoadingOverlay" Background="#80000000" 
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" 
              Panel.ZIndex="1000">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <mah:ProgressRing IsActive="True" Width="60" Height="60" Foreground="#FACB01"/>
                <TextBlock Text="Đang tải dữ liệu..." Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,15,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40">
                <!-- Header -->
                <TextBlock Text="Tạo bài tập mới" FontSize="28" FontWeight="Bold" Foreground="#191970" Margin="0,0,0,5"/>
                <TextBlock FontSize="16" Foreground="#666" Margin="0,0,0,30">
                    <Run Text="Lớp: "/>
                    <Run Text="{Binding ClassData.Name}" FontWeight="SemiBold"/>
                    <Run Text=" ("/>
                    <Run Text="{Binding ClassData.ClassCode}"/>
                    <Run Text=")"/>
                </TextBlock>

                <!-- Form -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <!-- Assignment Type -->
                        <TextBlock Text="Loại bài tập *" FontSize="16" FontWeight="SemiBold" Foreground="#191970" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding AssignmentTypes}"
                                  SelectedItem="{Binding AssignmentType}"
                                  mah:ControlsHelper.CornerRadius="8"
                                  Height="40" FontSize="16" Margin="0,0,0,20">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Converter={StaticResource AssignmentTypeDisplayConverter}}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>

                        <!-- Title -->
                        <TextBlock Text="Tên bài tập *" FontSize="16" FontWeight="SemiBold" Foreground="#191970" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                                 mah:TextBoxHelper.Watermark="Nhập tên bài tập"
                                 mah:ControlsHelper.CornerRadius="8"
                                 Height="40" FontSize="16" Padding="10" Margin="0,0,0,20"/>

                        <!-- Description -->
                        <TextBlock Text="Mô tả" FontSize="16" FontWeight="SemiBold" Foreground="#191970" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                 mah:TextBoxHelper.Watermark="Nhập mô tả bài tập (tùy chọn)"
                                 mah:ControlsHelper.CornerRadius="8"
                                 Height="120" FontSize="16" Padding="10" TextWrapping="Wrap"
                                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20"/>

                        <!-- Start Time -->
                        <TextBlock Text="Thời gian bắt đầu *" FontSize="16" FontWeight="SemiBold" Foreground="#191970" Margin="0,0,0,5"/>
                        <mah:DateTimePicker SelectedDateTime="{Binding StartTime}"
                                            mah:ControlsHelper.CornerRadius="8"
                                            Height="40" FontSize="14" Margin="0,0,0,20"
                                            Culture="vi-VN"
                                            PickerVisibility="All"
                                            HandVisibility="HourMinute"
                                            mah:TextBoxHelper.UseFloatingWatermark="False"/>

                        <!-- No End Date -->
                        <CheckBox IsChecked="{Binding NoEndDate}"
                                  Content="Không có thời gian kết thúc"
                                  FontSize="16" Foreground="#191970" FontWeight="SemiBold" Margin="0,0,0,15"/>

                        <!-- End Time -->
                        <TextBlock Text="Thời gian kết thúc" FontSize="16" FontWeight="SemiBold" Foreground="#191970" Margin="0,0,0,5"
                                   Visibility="{Binding IsEndDateEnabled, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <mah:DateTimePicker SelectedDateTime="{Binding EndTime}"
                                            mah:ControlsHelper.CornerRadius="8"
                                            Height="40" FontSize="14" Margin="0,0,0,20"
                                            Culture="vi-VN"
                                            IsEnabled="{Binding IsEndDateEnabled}"
                                            Visibility="{Binding IsEndDateEnabled, Converter={StaticResource BooleanToVisibilityConverter}}"
                                            PickerVisibility="All"
                                            HandVisibility="HourMinute"
                                            mah:TextBoxHelper.UseFloatingWatermark="False"/>

                        <!-- Allow Late Submission -->
                        <CheckBox IsChecked="{Binding AllowLateSubmission}"
                                  Content="Cho phép nộp bài muộn"
                                  FontSize="16" Foreground="#191970" FontWeight="SemiBold" Margin="0,0,0,20"/>

                        <!-- Info Alert -->
                        <Border Background="#E3F2FD" BorderBrush="#2196F3" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="Information" Foreground="#2196F3" Width="20" Height="20" Margin="0,0,10,0" VerticalAlignment="Top"/>
                                <TextBlock TextWrapping="Wrap" Foreground="#1976D2" FontSize="15">
                                    Sau khi tạo bài tập, bạn sẽ được chuyển đến trang quản lý bài tập để thêm các bài toán.
                                </TextBlock>
                            </StackPanel>
                        </Border>

                        <!-- Actions -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Command="{Binding CancelCommand}"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Width="120" Margin="0,0,15,0">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="Close" Width="16" Height="16" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Hủy" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding SaveCommand}"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBooleanConverter}}"
                                    Width="120">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="Check" Width="16" Height="16" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding IsSaving, Converter={StaticResource SavingTextConverter}}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Saving Overlay -->
        <Grid Background="#80000000" 
              Visibility="{Binding IsSaving, Converter={StaticResource BooleanToVisibilityConverter}}" 
              Panel.ZIndex="999">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <mah:ProgressRing IsActive="True" Width="60" Height="60" Foreground="#FACB01"/>
                <TextBlock Text="Đang tạo..." Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,15,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</mah:MetroWindow>

