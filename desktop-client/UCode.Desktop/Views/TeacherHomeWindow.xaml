<Window x:Class="UCode.Desktop.Views.TeacherHomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="UCode - Trang Giảng viên" 
        Height="900" Width="1600"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        WindowState="Maximized">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Navigation Bar -->
        <Border Grid.Row="0" Background="#003d8f" Height="60">
            <Grid Margin="40,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Back Button & Logo -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="BackButton" Foreground="White" Background="Transparent" 
                            BorderThickness="0" FontSize="14" FontWeight="SemiBold" 
                            Padding="15,0,20,0" Cursor="Hand" Height="60"
                            Click="BackButton_Click"
                            Visibility="Collapsed"
                            ToolTip="Quay lại">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="M15,12 L7,12 M10,15 L7,12 L10,9" Stroke="White" StrokeThickness="2" Width="18" Height="18" Stretch="Uniform" Margin="0,0,8,0"/>
                            <TextBlock Text="QUAY LẠI" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <TextBlock Text="📚" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="UCode" FontSize="24" FontWeight="Bold" Foreground="#FACB01" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Navigation Tabs -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0">
                    <Button x:Name="DashboardTab" Content="DASHBOARD" 
                            Foreground="#FACB01" Background="Transparent" BorderThickness="0,0,0,3" BorderBrush="#FACB01"
                            FontSize="13" FontWeight="Bold" Padding="20,0" Height="60" Cursor="Hand"
                            Click="DashboardTab_Click"/>
                    <Button x:Name="ProblemsTab" Content="NGÂN HÀNG BÀI" 
                            Foreground="White" Background="Transparent" BorderThickness="0" 
                            FontSize="13" FontWeight="Bold" Padding="20,0" Height="60" Cursor="Hand" Margin="20,0,0,0"
                            Click="ProblemsTab_Click"/>
                </StackPanel>

                <!-- User Info -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="👤" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="UserNameText" Text="Teacher" Foreground="White" FontSize="14" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area - Navigation Frame -->
        <!-- This will show either home content or navigated pages -->
        <ContentControl x:Name="NavigationFrame" Grid.Row="1"/>
    </Grid>
</Window>
