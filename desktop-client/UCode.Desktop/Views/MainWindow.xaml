<mah:MetroWindow x:Class="UCode.Desktop.Views.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                 Title="UCode - Trang chủ"
                 Height="800"
                 Width="1400"
                 WindowStartupLocation="CenterScreen"
                 WindowState="Maximized"
                 TitleCharacterCasing="Normal"
                 ShowIconOnTitleBar="True"
                 GlowBrush="{DynamicResource MahApps.Brushes.Accent}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Navigation Bar -->
        <Border Grid.Row="0"
                Background="#1976D2"
                Height="64"
                Panel.ZIndex="10">
            <Border.Effect>
                <DropShadowEffect Color="Black"
                                  BlurRadius="10"
                                  ShadowDepth="2"
                                  Opacity="0.2"/>
            </Border.Effect>
            <Grid Margin="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo & Title -->
                <StackPanel Grid.Column="0"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <Border Background="White"
                            Width="40"
                            Height="40"
                            CornerRadius="8"
                            Margin="0,0,12,0">
                        <iconPacks:PackIconMaterial Kind="CodeBraces"
                                                    Width="24"
                                                    Height="24"
                                                    Foreground="#1976D2"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="UCode"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="White"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Navigation Menu -->
                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Left"
                            Margin="60,0,0,0"
                            VerticalAlignment="Center">
                    <Button Style="{DynamicResource MahApps.Styles.Button.Flat}"
                            Foreground="White"
                            FontSize="15"
                            Margin="0,0,8,0"
                            Height="40"
                            Padding="16,0"
                            Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Home"
                                                        Width="18"
                                                        Height="18"
                                                        VerticalAlignment="Center"
                                                        Margin="0,0,8,0"/>
                            <TextBlock Text="Trang chủ"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{DynamicResource MahApps.Styles.Button.Flat}"
                            Foreground="White"
                            FontSize="15"
                            Margin="0,0,8,0"
                            Height="40"
                            Padding="16,0"
                            Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="GoogleClassroom"
                                                        Width="18"
                                                        Height="18"
                                                        VerticalAlignment="Center"
                                                        Margin="0,0,8,0"/>
                            <TextBlock Text="Lớp học"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{DynamicResource MahApps.Styles.Button.Flat}"
                            Foreground="White"
                            FontSize="15"
                            Height="40"
                            Padding="16,0"
                            Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Brain"
                                                        Width="18"
                                                        Height="18"
                                                        VerticalAlignment="Center"
                                                        Margin="0,0,8,0"/>
                            <TextBlock Text="Luyện tập"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- User Menu -->
                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <Border Background="#1565C0"
                            CornerRadius="20"
                            Padding="12,6"
                            Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Account"
                                                        Width="18"
                                                        Height="18"
                                                        Foreground="White"
                                                        VerticalAlignment="Center"
                                                        Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding UserEmail}"
                                       Foreground="White"
                                       FontSize="14"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Button Style="{DynamicResource MahApps.Styles.Button.Flat}"
                            Foreground="White"
                            FontSize="14"
                            Command="{Binding LogoutCommand}"
                            Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Logout"
                                                        Width="18"
                                                        Height="18"
                                                        VerticalAlignment="Center"
                                                        Margin="0,0,6,0"/>
                            <TextBlock Text="Đăng xuất"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Background="#F5F5F5">
            <StackPanel Margin="40,30">

                <!-- Welcome Section -->
                <TextBlock FontSize="32"
                           FontWeight="Bold"
                           Margin="0,0,0,5">
                    <Run Text="Xin chào, "/>
                    <Run Text="{Binding UserName}"/>
                    <Run Text="! 👋"/>
                </TextBlock>
                <TextBlock Text="Hôm nay bạn muốn học gì?"
                           FontSize="16"
                           Foreground="#666"
                           Margin="0,0,0,30"/>

                <!-- Lớp học của tôi -->
                <Grid Margin="0,0,0,40">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Section Header -->
                    <StackPanel Grid.Row="0"
                                Orientation="Horizontal"
                                Margin="0,0,0,15">
                        <iconPacks:PackIconMaterial Kind="GoogleClassroom"
                                                    Width="28"
                                                    Height="28"
                                                    Foreground="{DynamicResource MahApps.Brushes.Accent}"
                                                    VerticalAlignment="Center"
                                                    Margin="0,0,10,0"/>
                        <TextBlock Text="Lớp học của tôi"
                                   FontSize="24"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Classes Grid -->
                    <ItemsControl Grid.Row="1"
                                  ItemsSource="{Binding Classes}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="3"
                                             HorizontalAlignment="Left"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White"
                                        BorderBrush="#E0E0E0"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Margin="0,0,20,20"
                                        Width="350"
                                        Height="250"
                                        Cursor="Hand">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#E0E0E0"
                                                          BlurRadius="10"
                                                          ShadowDepth="2"
                                                          Opacity="0.5"/>
                                    </Border.Effect>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="120"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <!-- Class Header -->
                                        <Border Grid.Row="0"
                                                Background="{DynamicResource MahApps.Brushes.Accent}"
                                                CornerRadius="8,8,0,0">
                                            <TextBlock Text="{Binding Code}"
                                                       FontSize="22"
                                                       FontWeight="Bold"
                                                       Foreground="White"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Border>

                                        <!-- Class Info -->
                                        <StackPanel Grid.Row="1"
                                                    Margin="20">
                                            <TextBlock Text="{Binding Name}"
                                                       FontSize="18"
                                                       FontWeight="SemiBold"
                                                       TextWrapping="Wrap"
                                                       Margin="0,0,0,8"/>
                                            <TextBlock Text="{Binding TeacherName}"
                                                       FontSize="14"
                                                       Foreground="#666"
                                                       Margin="0,0,0,8"/>
                                            <Border Background="#E3F2FD"
                                                    CornerRadius="12"
                                                    Padding="10,5"
                                                    HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding Semester}"
                                                           FontSize="12"
                                                           Foreground="#1976D2"/>
                                            </Border>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>

                <!-- Việc cần làm -->
                <Grid Margin="0,0,0,40">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Section Header -->
                    <StackPanel Grid.Row="0"
                                Orientation="Horizontal"
                                Margin="0,0,0,15">
                        <iconPacks:PackIconMaterial Kind="ClockAlertOutline"
                                                    Width="28"
                                                    Height="28"
                                                    Foreground="#F44336"
                                                    VerticalAlignment="Center"
                                                    Margin="0,0,10,0"/>
                        <TextBlock Text="Việc cần làm"
                                   FontSize="24"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                        <Border Background="#FFEBEE"
                                CornerRadius="12"
                                Padding="8,4"
                                Margin="15,0,0,0">
                            <TextBlock FontSize="12"
                                       Foreground="#F44336">
                                <Run Text="{Binding UpcomingAssignmentsCount, Mode=OneWay}"/>
                                <Run Text=" bài tập"/>
                            </TextBlock>
                        </Border>
                    </StackPanel>

                    <!-- Assignments List -->
                    <ItemsControl Grid.Row="1"
                                  ItemsSource="{Binding UpcomingAssignments}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White"
                                        BorderBrush="#E0E0E0"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Margin="0,0,0,15"
                                        Padding="20"
                                        Cursor="Hand">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Icon -->
                                        <Border Grid.Column="0"
                                                Background="#FFEBEE"
                                                Width="48"
                                                Height="48"
                                                CornerRadius="8"
                                                Margin="0,0,15,0">
                                            <iconPacks:PackIconMaterial Kind="ClipboardText"
                                                                        Width="28"
                                                                        Height="28"
                                                                        Foreground="#F44336"
                                                                        HorizontalAlignment="Center"
                                                                        VerticalAlignment="Center"/>
                                        </Border>

                                        <!-- Info -->
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Title}"
                                                       FontSize="18"
                                                       FontWeight="SemiBold"
                                                       Margin="0,0,0,5"/>
                                            <TextBlock Text="{Binding ClassName}"
                                                       FontSize="14"
                                                       Foreground="#666"
                                                       Margin="0,0,0,10"/>
                                            <StackPanel Orientation="Horizontal">
                                                <Border Background="#FFEBEE"
                                                        CornerRadius="12"
                                                        Padding="8,4"
                                                        Margin="0,0,10,0">
                                                    <StackPanel Orientation="Horizontal">
                                                        <iconPacks:PackIconMaterial Kind="ClockOutline"
                                                                                    Width="14"
                                                                                    Height="14"
                                                                                    Foreground="#F44336"
                                                                                    VerticalAlignment="Center"
                                                                                    Margin="0,0,5,0"/>
                                                        <TextBlock FontSize="12"
                                                                   Foreground="#F44336">
                                                            <Run Text="Còn"/>
                                                            <Run Text="{Binding DaysLeft}"/>
                                                            <Run Text="ngày"/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Border>
                                                <Border BorderBrush="#E0E0E0"
                                                        BorderThickness="1"
                                                        CornerRadius="12"
                                                        Padding="8,4"
                                                        Margin="0,0,10,0">
                                                    <TextBlock FontSize="12"
                                                               Foreground="#666">
                                                        <Run Text="{Binding ProblemCount}"/>
                                                        <Run Text="bài"/>
                                                    </TextBlock>
                                                </Border>
                                                <Border BorderBrush="#E0E0E0"
                                                        BorderThickness="1"
                                                        CornerRadius="12"
                                                        Padding="8,4">
                                                    <TextBlock FontSize="12"
                                                               Foreground="#666">
                                                        <Run Text="{Binding TotalPoints}"/>
                                                        <Run Text="điểm"/>
                                                    </TextBlock>
                                                </Border>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>

                <!-- Luyện tập -->
                <Grid Margin="0,0,0,40">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Section Header -->
                    <StackPanel Grid.Row="0"
                                Orientation="Horizontal"
                                Margin="0,0,0,15">
                        <iconPacks:PackIconMaterial Kind="CodeBraces"
                                                    Width="28"
                                                    Height="28"
                                                    Foreground="#4CAF50"
                                                    VerticalAlignment="Center"
                                                    Margin="0,0,10,0"/>
                        <TextBlock Text="Luyện tập"
                                   FontSize="24"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Practice Categories Grid -->
                    <ItemsControl Grid.Row="1"
                                  ItemsSource="{Binding PracticeCategories}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="4"
                                             HorizontalAlignment="Left"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White"
                                        BorderBrush="#E0E0E0"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Margin="0,0,20,20"
                                        Width="280"
                                        Padding="20"
                                        Cursor="Hand">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Icon}"
                                                   FontSize="36"
                                                   Margin="0,0,0,10"/>
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="18"
                                                   FontWeight="SemiBold"
                                                   Margin="0,0,0,8"/>
                                        <TextBlock Text="{Binding Description}"
                                                   FontSize="13"
                                                   Foreground="#666"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,15"/>
                                        <Border Background="#E8F5E9"
                                                BorderBrush="#4CAF50"
                                                BorderThickness="1"
                                                CornerRadius="12"
                                                Padding="8,4"
                                                HorizontalAlignment="Left">
                                            <TextBlock FontSize="12"
                                                       Foreground="#2E7D32">
                                                <Run Text="{Binding ProblemCount}"/>
                                                <Run Text="bài"/>
                                            </TextBlock>
                                        </Border>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>

            </StackPanel>
        </ScrollViewer>

        <!-- Loading Overlay -->
        <Border Grid.Row="0"
                Grid.RowSpan="2"
                Background="#DD000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                Panel.ZIndex="999">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <mah:ProgressRing IsActive="True"
                                  Width="80"
                                  Height="80"
                                  Foreground="White"
                                  Margin="0,0,0,20"/>
                <TextBlock Text="Đang tải dữ liệu..."
                           FontSize="18"
                           Foreground="White"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</mah:MetroWindow>
