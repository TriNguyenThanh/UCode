<mah:MetroWindow x:Class="UCode.Desktop.Views.TeacherAssignmentEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        Title="Chỉnh sửa bài tập - UCode" 
        Height="824" Width="1195"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        ResizeMode="NoResize"
        ShowMinButton="False"
        ShowMaxRestoreButton="False">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Loading Overlay 
        <Grid x:Name="LoadingOverlay" Background="#80000000" 
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" 
              Panel.ZIndex="1000" Grid.RowSpan="2">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Foreground="#FACB01"/>
                <TextBlock Text="Đang tải dữ liệu..." Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,15,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
            -->
        <!-- Main Content Area (Scrollable) -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
            <Grid Margin="30,30,30,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Thông tin bài tập" FontSize="24" FontWeight="Bold" Foreground="#191970" Margin="0,0,0,5"/>
                    <TextBlock Text="Điền đầy đủ thông tin bài tập và chọn các bài toán" FontSize="14" Foreground="#666"/>
                </StackPanel>

                <!-- Main Content -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left Column: Form Fields -->
                    <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,14,-21">
                        <StackPanel>
                            <!-- Class -->

                            <!-- Title -->
                            <TextBlock Text="Tiêu đề *" FontSize="14" FontWeight="SemiBold" Foreground="#191970" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" 
                                     Height="40" FontSize="14" Padding="10" Margin="0,0,0,20"/>

                            <!-- Description -->
                            <TextBlock Text="Mô tả" FontSize="14" FontWeight="SemiBold" Foreground="#191970" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                                     Height="100" FontSize="14" Padding="10" 
                                     TextWrapping="Wrap" AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto" Margin="0,0,0,20"/>

                            <!-- Assignment Type -->
                            <TextBlock Text="Loại bài tập *" FontSize="14" FontWeight="SemiBold" Foreground="#191970" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding AssignmentTypes}" 
                                      SelectedItem="{Binding AssignmentType}" 
                                      Height="40" FontSize="14" Margin="0,0,0,20"/>

                            <!-- Status -->
                            <TextBlock Text="Trạng thái *" FontSize="14" FontWeight="SemiBold" Foreground="#191970" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding Statuses}" 
                                      SelectedItem="{Binding Status}" 
                                      Height="40" FontSize="14" Margin="0,0,0,20"/>

                            <!-- Start Time -->
                            <TextBlock Text="Thời gian bắt đầu" FontSize="14" FontWeight="SemiBold" Foreground="#191970" Margin="0,0,0,5"/>
                            <DatePicker SelectedDate="{Binding StartTime}" 
                                        Height="40" FontSize="14" Margin="0,0,0,20"/>

                            <!-- End Time -->
                            <TextBlock Text="Thời gian kết thúc" FontSize="14" FontWeight="SemiBold" Foreground="#191970" Margin="0,0,0,5"/>
                            <DatePicker SelectedDate="{Binding EndTime}" 
                                        Height="40" FontSize="14" Margin="0,0,0,20"/>

                            <!-- Allow Late Submission -->
                            <CheckBox IsChecked="{Binding AllowLateSubmission}" 
                                      Content="Cho phép nộp trễ" 
                                      FontSize="14" Foreground="#191970" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>

                    <!-- Right Column: Problems Selection -->
                    <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="16,0,10,-21">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Header with Add Button -->
                            <Grid Grid.Row="0" Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="Danh sách đề bài" FontSize="16" FontWeight="Bold" Foreground="#191970" VerticalAlignment="Center"/>

                                <Button Grid.Column="1" 
                                        Command="{Binding AddProblemCommand}"
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Padding="15,8"
                                        Cursor="Hand" HorizontalAlignment="Left" Width="107">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="+" FontSize="18" Margin="0,0,5,0" VerticalAlignment="Center" RenderTransformOrigin="0.637,0.418" Height="26" Width="13"/>
                                        <TextBlock Text="THÊM BÀI" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </Grid>

                            <!-- Selected Problems List with Scroll -->
                            <Border Grid.Row="1" Background="#F8F9FA" CornerRadius="6">
                                <ScrollViewer VerticalScrollBarVisibility="Auto"  MaxHeight="600">
                                    <Grid Height="Auto">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="300*"/>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="300*"/>
                                        </Grid.ColumnDefinitions>
                                        <!-- Empty State 
                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left"
                                                    Visibility="{Binding HasSelectedProblems, Converter={StaticResource InverseBooleanToVisibilityConverter}}" Height="118" Width="299" Grid.ColumnSpan="3" Margin="150,0,0,0">
                                            <TextBlock Text="📝" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                            <TextBlock Text="Chưa có đề bài nào" FontSize="16" FontWeight="SemiBold" Foreground="#666" HorizontalAlignment="Center"/>
                                            <TextBlock Text="Nhấn nút 'Thêm bài' để chọn đề bài cho assignment" FontSize="13" Foreground="#999" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                        </StackPanel> -->

                                        <!-- Problems List -->
                                        <ItemsControl ItemsSource="{Binding SelectedProblems}"
                                                      Visibility="{Binding HasSelectedProblems, Converter={StaticResource BooleanToVisibilityConverter}}" Grid.ColumnSpan="3" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Margin="10,10,10,10">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="White" CornerRadius="4" Padding="8"
                                                            BorderThickness="1" BorderBrush="#E0E0E0">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>

                                                            <TextBlock Grid.Column="0" Text="{Binding OrderIndex}" 
                                                                       FontSize="16" FontWeight="Bold" Foreground="#FACB01" 
                                                                       VerticalAlignment="Center" Margin="0,0,12,0"/>

                                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                                <TextBlock Text="{Binding Title}" FontSize="13" FontWeight="SemiBold" Foreground="#191970"/>
                                                                <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                                                    <TextBlock Text="{Binding Code}" FontSize="11" Foreground="#666" Margin="0,0,10,0"/>
                                                                    <TextBlock Text="•" FontSize="11" Foreground="#666" Margin="0,0,5,0"/>
                                                                    <TextBox Text="{Binding Points, UpdateSourceTrigger=PropertyChanged}" 
                                                                             Width="50" Height="24" FontSize="11" Padding="5,0"/>
                                                                    <TextBlock Text=" điểm" FontSize="11" Foreground="#666" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                                                </StackPanel>
                                                            </StackPanel>

                                                            <StackPanel Grid.Column="2" Orientation="Vertical" Margin="8,0">
                                                                <Button Width="30" Height="24" Padding="0" Margin="0,0,0,4"
                                                                        Command="{Binding DataContext.MoveProblemUpCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                        CommandParameter="{Binding ProblemId}"
                                                                        ToolTip="Di chuyển lên">
                                                                    <Path Data="M7,15 L12,10 L17,15" Stroke="#666" StrokeThickness="2" Width="16" Height="16" Stretch="Uniform"/>
                                                                </Button>
                                                                <Button Width="30" Height="24" Padding="0"
                                                                        Command="{Binding DataContext.MoveProblemDownCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                        CommandParameter="{Binding ProblemId}"
                                                                        ToolTip="Di chuyển xuống">
                                                                    <Path Data="M7,10 L12,15 L17,10" Stroke="#666" StrokeThickness="2" Width="16" Height="16" Stretch="Uniform"/>
                                                                </Button>
                                                            </StackPanel>

                                                            <Button Grid.Column="3" Content="✕" Width="30" Height="30" 
                                                                    FontSize="16" Padding="0" 
                                                                    Command="{Binding DataContext.RemoveProblemCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                    CommandParameter="{Binding ProblemId}"
                                                                    Style="{StaticResource DangerButtonStyle}"/>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Grid>
                                </ScrollViewer>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </ScrollViewer>

        <!-- Fixed Footer with Actions -->
        <Border Grid.Row="1" 
                Background="White" 
                BorderThickness="0,1,0,0" 
                BorderBrush="#E0E0E0"
                Padding="30,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Hủy" 
                        Style="{StaticResource SecondaryButtonStyle}" 
                        Command="{Binding CancelCommand}" 
                        Width="120" 
                        Height="40"
                        Margin="0,0,15,0"/>
                <Button Content="Lưu" 
                        Style="{StaticResource PrimaryButtonStyle}" 
                        Command="{Binding SaveCommand}" 
                        IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBooleanConverter}}"
                        Width="120"
                        Height="40"/>
            </StackPanel>
        </Border>

        <!-- Saving Overlay 
        <Grid Grid.RowSpan="2" Background="#80000000" 
              Visibility="{Binding IsSaving, Converter={StaticResource BooleanToVisibilityConverter}}" 
              Panel.ZIndex="999">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Foreground="#FACB01"/>
                <TextBlock Text="Đang lưu..." Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,15,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid> -->
    </Grid>
</mah:MetroWindow>

