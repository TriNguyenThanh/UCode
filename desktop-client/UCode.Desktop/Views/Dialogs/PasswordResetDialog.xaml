<mah:MetroWindow x:Class="UCode.Desktop.Views.Dialogs.PasswordResetDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                 Title="Reset Password" 
                 Height="350" 
                 Width="500"
                 WindowStartupLocation="CenterOwner"
                 GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
                 ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Reset Password" 
                      FontSize="24"
                      FontWeight="Bold"
                      Foreground="#FF6F00"/>
            <TextBlock Text="{Binding UserInfo}" 
                      FontSize="14"
                      Foreground="#666"
                      Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Form Content -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Enter new password for this user:"
                      FontSize="14"
                      Foreground="#333"
                      Margin="0,0,0,15"/>

            <!-- New Password -->
            <PasswordBox x:Name="NewPasswordBox"
                        materialDesign:HintAssist.Hint="New Password *"
                        Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                        PasswordChanged="PasswordBox_PasswordChanged"
                        Margin="0,0,0,15"/>

            <!-- Confirm Password -->
            <PasswordBox x:Name="ConfirmPasswordBox"
                        materialDesign:HintAssist.Hint="Confirm Password *"
                        Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                        Margin="0,0,0,15"/>

            <!-- Password Requirements -->
            <Border Background="#FFF3E0" 
                   BorderBrush="#FFB74D"
                   BorderThickness="1"
                   CornerRadius="4"
                   Padding="10"
                   Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="Password Requirements:" 
                              FontWeight="SemiBold"
                              FontSize="12"
                              Foreground="#E65100"
                              Margin="0,0,0,5"/>
                    <TextBlock FontSize="11" Foreground="#666">
                        <Run Text="• Minimum 6 characters"/>
                        <LineBreak/>
                        <Run Text="• Recommended: Mix of letters, numbers, and symbols"/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Validation Message -->
            <TextBlock Text="{Binding ValidationMessage}"
                      Foreground="#D32F2F"
                      FontSize="12"
                      TextWrapping="Wrap"
                      Margin="0,0,0,10"
                      Visibility="{Binding HasValidationError, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>

        <!-- Action Buttons -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1"
                   Content="Cancel"
                   Style="{StaticResource MaterialDesignOutlinedButton}"
                   Width="120"
                   Margin="0,0,10,0"
                   Click="CancelButton_Click"/>

            <Button Grid.Column="2"
                   Content="Reset Password"
                   Style="{StaticResource MaterialDesignRaisedButton}"
                   Width="140"
                   Click="ResetButton_Click"
                   IsEnabled="{Binding CanReset}"/>
        </Grid>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="3"
              Background="#80000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                            IsIndeterminate="True"
                            Width="50"
                            Height="50"/>
                <TextBlock Text="Resetting password..."
                          Foreground="White"
                          FontSize="14"
                          Margin="0,15,0,0"
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</mah:MetroWindow>
